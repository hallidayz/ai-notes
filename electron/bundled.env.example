# Optional defaults baked into the desktop installer.
# Copy to bundled.env only for shared non-secret defaults.
# Users can override via %APPDATA%/Acaiguardian/.env (Windows) or ~/Library/Application Support/Acaiguardian/.env (macOS).
#
# GEMINI_API_KEY=your-key-here
